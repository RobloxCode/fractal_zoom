[package]
name = "fractal_zoom"
version = "0.1.0"
edition = "2021"

[dependencies]
minifb = { version = "0.24", default-features = false, features = ["x11"] }

[profile.release]
opt-level = 3
lto = true
